<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" package="com.tsymiar.device2device">

    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADVERTISE" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" tools:ignore="ScopedStorage" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_MULTICAST_STATE" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.MOUNT_UNMOUNT_FILESYSTEMS" tools:ignore="ProtectedPermissions" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.HIGH_SAMPLING_RATE_SENSORS"
        tools:ignore="HighSamplingRate" />

    <application android:allowBackup="true" android:icon="@drawable/ic_launcher" android:label="@string/app_name" android:roundIcon="@drawable/ic_launcher" android:supportsRtl="true" android:theme="@style/AppTheme" android:requestLegacyExternalStorage="true" tools:ignore="AllowBackup,GoogleAppIndexingWarning">

        <activity android:name=".activity.MainActivity" android:exported="true"
            tools:ignore="MissingClass">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.SelectActivity"
            android:exported="true" android:theme="@style/Theme.MaterialComponents.DayNight.DarkActionBar">
            <intent-filter>
                <action android:name="android.intent.action.CONNECTION" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.TextureActivity" />
        <activity
            android:name=".activity.MarketActivity"
            android:label="@string/market_quote" />
        <activity
            android:name=".activity.AvatarActivity"
            android:label="@string/avatar_3d" />

        <!-- 行情桌面小部件：配置页由桌面在添加小部件时拉起 -->
        <receiver
            android:name=".widget.MarketWidgetProvider"
            android:exported="true"
            android:label="@string/market_widget_label">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>

            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/market_widget_info" />
        </receiver>
        <activity
            android:name=".widget.MarketWidgetConfigActivity"
            android:exported="true"
            android:label="@string/market_widget_config"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_CONFIGURE" />
            </intent-filter>
        </activity>
        <!-- 行情小部件列表的数据源：标的行装在 ListView 里，超出高度可滚动。
             BIND_REMOTEVIEWS 权限保证只有桌面能绑定它 -->
        <service
            android:name=".widget.MarketWidgetService"
            android:exported="true"
            android:permission="android.permission.BIND_REMOTEVIEWS" />
        <activity android:name=".activity.WaveActivity" android:theme="@style/Theme.MaterialComponents.DayNight.DarkActionBar" />
        <activity android:name=".activity.GraphActivity" />
        <activity android:name=".activity.SensorActivity" />
        <service
            android:name=".service.PublishService"
            android:theme="@style/Theme.MaterialComponents.Light.Dialog" />
        <service android:name=".service.SubscribeService" />
        <service
            android:name=".service.HttpServerService"
            android:exported="false" />
        <service android:name=".service.ToastNotificationService" />
        <service android:name=".acceleration.Voice.Voice"
            tools:ignore="MissingClass" />
        <service android:name=".service.WindowService" />
        <service android:name=".service.ReceiverService" />
        <service android:name=".service.SaveDataService" />
        <activity
            android:name=".activity.ConnectActivity"
            android:exported="true"
            android:label="@string/title"
            android:theme="@style/BluetoothDialog" />
        <activity
            android:name=".activity.CommitActivity"
            android:alwaysRetainTaskState="true"
            android:exported="true"
            android:label="@string/window"
            android:theme="@android:style/Theme.Holo.Dialog"
            tools:ignore="NewApi">
            <intent-filter>
                <action android:name="android.intent.action.NEW" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activity.DevicesActivity"
            android:configChanges="orientation|keyboardHidden"
            android:label="@string/title_activity_e"
            android:screenOrientation="fullSensor"
            android:theme="@android:style/Theme.Holo.Dialog"
            tools:ignore="NewApi" />

        <activity android:name=".activity.BuggerActivity" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <action android:name="android.intent.action.SENDTO" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data android:scheme="mail's" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activity.MyGitActivity"
            android:exported="true"
            android:screenOrientation="unspecified">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="file" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activity.ThanksActivity"
            android:configChanges="orientation|keyboardHidden"
            android:label="@string/thank"
            android:theme="@android:style/Theme.Material.Dialog.Alert"
            tools:ignore="NewApi" />

        <!-- 拍照：把 app 私有目录的临时文件授权给系统相机应用写入 -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="com.tsymiar.device2device.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>

</manifest>
